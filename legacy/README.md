# Legacy

Frozen earlier SpaceY experiments, left in place for reference.

This project was always an experiment in ML and browser-based training. The live gym is the TypeScript app at the repo root (`src/game`). Nothing in this folder is built, served, or trained.

- [`rust/`](rust/) — previous WASM physics + CMA-NeuroES trainer (snapshot of `bcab81b`). See [`docs/legacy.md`](../docs/legacy.md).
