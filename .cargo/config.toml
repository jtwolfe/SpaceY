# Match wasm32 (no hardware FMA) so native `cargo test` integrates the
# same LEO skip as the browser build.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "target-feature=-fma"]

[target.aarch64-unknown-linux-gnu]
rustflags = ["-C", "target-feature=-fma"]
